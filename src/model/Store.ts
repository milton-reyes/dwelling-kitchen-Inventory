class Store {
    id: number;
    stock: number;

    name: string;

    constructor(id: number,stock: number,name: string) {
        this.id = id;
        this.stock = stock;

        this.name = name;

    }

}